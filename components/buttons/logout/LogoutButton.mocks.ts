import { ILogoutButton } from './LogoutButton';

const base: ILogoutButton = {
  text: 'Hello world!',
};

const alt: ILogoutButton = {
  text: 'Hello alt world!',
};

export const mockLogoutButtonProps = {
  base,
  alt,
};
